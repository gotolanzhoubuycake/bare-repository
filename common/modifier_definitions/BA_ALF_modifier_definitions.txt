##宜居度：一个整数标识符。
##它主要会被用在精神修正里用于计算。
ALF_Livability_int = {
	color_type = good
	value_type = number
	precision = 2
}

##军事改革进度##
ALF_military_reform_int = {
	color_type = good
	value_type = percentage
	precision = 2
}