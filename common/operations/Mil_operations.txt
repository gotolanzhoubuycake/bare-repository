 operation_capture_cipher = { ## 3 1/55
 	icon = GFX_operations_capture_ciphers
 	map_icon = GFX_operations_capture_ciphers_map
 	name = operation_capture_cipher
 	desc = operation_capture_cipher_desc
 	priority = 3

 	days = 45
 	network_strength = 40
 	operatives = 1
 	is_captured_cipher = yes

 	visible = {
        ROOT = {
            tag = MIL
        }
 		network_strength = {
 			target = FROM
 			value > 25
 		}
 		has_done_agency_upgrade = upgrade_form_department
 		NOT = {
 			ROOT = { is_fully_decrypted = FROM }
 		}
 	}

 	equipment = {
		
 	}
 	risk_chance = 0.2
 	outcome_extra_chance = 0.25
 	outcome_execute = {
 		ROOT = {
 			add_decryption = {
 				target = FROM
 				ratio = 0.5
 			}
            random_list = {
 				25 = {
 					add_intel = {
 						target = FROM
 						civilian_intel = 10
 					}
 				}
 				25 = {
 					add_intel = {
 						target = FROM
 						army_intel = 10
 					}
 				}
 				25 = {
 					add_intel = {
 						target = FROM
 						airforce_intel = 10
 					}
 				}
 			}
 		}
 	}
 	outcome_extra_execute = {    
 		random_list = {
 			33 = {
 				ROOT = {
 					add_decryption = {
 						target = FROM
 						ratio = 0.5
 					}
 					random_list = {
 						25 = {
 							add_intel = {
 								target = FROM
 								civilian_intel = 30
 							}
 						}
 						25 = {
 							add_intel = {
 								target = FROM
 								army_intel = 30
 							}
 						}
 						25 = {
 							add_intel = {
 								target = FROM
 								airforce_intel = 30
 							}
 						}
 					}
 				}
 			}
 			67 = {
 				ROOT = {
 					add_decryption = {
 						target = FROM
 						ratio = 0.6
 					}
 				}
 			}
 		}	
 	}

 	outcome_potential = {
 		ROOT = {
 			add_decryption = {
 				target = FROM
 				ratio = 0.3
 			}
 		}
 	}

 	phases = { #infiltration
 		infiltration_border = { base = 50 }
 		infiltration_paradrop = { 
 			base = 50 
 		}
 	}
 	phases = { #capturing cyphers
 		capture_cyphers_raid = {
 			base = 50
 			modifier = {
 				ROOT = { has_war = no }
 				factor = 0
 			}
 		}
 		capture_cyphers_reverse_engineer = { base = 50 }
 	}

 	phases = { #exfiltration
 		exfiltration_air_pickup = { 
 			base = 50 
 
 		}
 		exfiltration_border = { base = 50 }
 	}
	
 }




MIL_operation_box_weapon = {
    icon = GFX_operation_box_weapon
	map_icon = GFX_operation_box_weapon_map
    name = MIL_operation_box_weapon
    desc = MIL_operation_box_weapon_desc
    priority = 5
    days = 30
    network_strength = 0
    operatives = 1

    visible = {
        FROM = { has_completed_focus = MIL_Networked_Weapons_Necessary }
    }

    available = {
        FROM = { has_completed_focus = MIL_Networked_Weapons_Necessary }
    }

    target_weight = {
        base = 0
        modifier = {
            add = 1
            FROM = {
                OR = {
                    has_war_with = ROOT
                    has_hostile_attitude = ROOT
                }
            }
        }
    }

    cost = {
        civilian_factory_use = 15
    }

    risk_chance = 0.40
    outcome_extra_chance = 0.20

    phases = {
        MIL_box_weapon_phase_1 = {
            ai_will_do = { base = 1 }
            training_scale = 1.0
        }
    }

    risk_execute = { }

	outcome_execute = {
		FROM = {
			add_relation_modifier = {
				target = ROOT
				modifier = MIL_box_weapon_success_targeted
			}
		}
	}

	outcome_extra_execute = {
		FROM = {
			add_relation_modifier = {
				target = ROOT
				modifier = MIL_box_weapon_great_success_targeted
			}
		}
	}

    experience = 0
    will_lead_to_war = no
}
