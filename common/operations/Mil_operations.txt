 operation_capture_cipher = { ## 3 1/55
 	icon = GFX_operations_capture_ciphers
 	map_icon = GFX_operations_capture_ciphers_map
 	name = operation_capture_cipher
 	desc = operation_capture_cipher_desc
 	priority = 3

 	days = 45
 	network_strength = 40
 	operatives = 1
 	is_captured_cipher = yes

 	visible = {
        ROOT = {
            tag = MIL
        }
 		network_strength = {
 			target = FROM
 			value > 25
 		}
 		has_done_agency_upgrade = upgrade_form_department
 		NOT = {
 			ROOT = { is_fully_decrypted = FROM }
 		}
 	}

 	equipment = {
		
 	}
 	risk_chance = 0.2
 	outcome_extra_chance = 0.25
 	outcome_execute = {
 		ROOT = {
 			add_decryption = {
 				target = FROM
 				ratio = 0.5
 			}
            random_list = {
 				25 = {
 					add_intel = {
 						target = FROM
 						civilian_intel = 10
 					}
 				}
 				25 = {
 					add_intel = {
 						target = FROM
 						army_intel = 10
 					}
 				}
 				25 = {
 					add_intel = {
 						target = FROM
 						airforce_intel = 10
 					}
 				}
 			}
 		}
 	}
 	outcome_extra_execute = {    
 		random_list = {
 			33 = {
 				ROOT = {
 					add_decryption = {
 						target = FROM
 						ratio = 0.5
 					}
 					random_list = {
 						25 = {
 							add_intel = {
 								target = FROM
 								civilian_intel = 30
 							}
 						}
 						25 = {
 							add_intel = {
 								target = FROM
 								army_intel = 30
 							}
 						}
 						25 = {
 							add_intel = {
 								target = FROM
 								airforce_intel = 30
 							}
 						}
 					}
 				}
 			}
 			67 = {
 				ROOT = {
 					add_decryption = {
 						target = FROM
 						ratio = 0.6
 					}
 				}
 			}
 		}	
 	}

 	outcome_potential = {
 		ROOT = {
 			add_decryption = {
 				target = FROM
 				ratio = 0.3
 			}
 		}
 	}

 	phases = { #infiltration
 		infiltration_border = { base = 50 }
 		infiltration_paradrop = { 
 			base = 50 
 		}
 	}
 	phases = { #capturing cyphers
 		capture_cyphers_raid = {
 			base = 50
 			modifier = {
 				ROOT = { has_war = no }
 				factor = 0
 			}
 		}
 		capture_cyphers_reverse_engineer = { base = 50 }
 	}

 	phases = { #exfiltration
 		exfiltration_air_pickup = { 
 			base = 50 
 
 		}
 		exfiltration_border = { base = 50 }
 	}
	
 }




MIL_operation_box_weapon = {
    icon = GFX_operation_box_weapon
    map_icon = GFX_operation_box_weapon_map
    name = MIL_operation_box_weapon
    desc = MIL_operation_box_weapon_desc
    priority = 5

    days = 30
    network_strength = 25
    operatives = 1

    visible = {
        ROOT = { has_completed_focus = MIL_Networked_Weapons_Necessary }
        network_national_coverage = {
            target = FROM
            value > 0
        }
    }

    available = {
        ROOT = { has_completed_focus = MIL_Networked_Weapons_Necessary }
    }

    equipment = {
        civilian_factories = 15
    }

    risk_chance = 0.40
    outcome_extra_chance = 0.20

		risk_modifiers = {
			operation_risk
		}

		outcome_modifiers = {
			operation_outcome
		}

		cost_modifiers = {
			operation_cost
		}

		awarded_tokens = {
			MIL_token_box_weapon_success
		}

    outcome_execute = {
        ROOT = {
            add_operation_token = {
                tag = FROM
                token = MIL_token_box_weapon_success
            }
        }
    }

    outcome_extra_execute = {
        ROOT = {
            add_operation_token = {
                tag = FROM
                token = MIL_token_box_weapon_great_success
            }
        }
    }

    outcome_potential = {
        ROOT = {
            add_operation_token = {
                tag = FROM
                token = MIL_token_box_weapon_success
            }
        }
    }

    risk_execute = { }

    phases = {
        MIL_box_weapon_phase_infiltration = { base = 50 }
        MIL_box_weapon_phase_infiltration_network = {
            base = 50
            modifier = {
                factor = 3
                ROOT = { has_war = no }
                FROM = { has_war = no }
            }
        }
    }

    phases = {
        MIL_box_weapon_phase_execute = { base = 100 }
    }

    phases = {
        MIL_box_weapon_phase_exfiltration_border = { base = 50 }
        MIL_box_weapon_phase_exfiltration_go_to_ground = { base = 50 }
    }
}
